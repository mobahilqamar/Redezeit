# Redezeit Timer für OBS

Dateien:
- `controller.html` = Steuerung
- `output.html` = OBS Browser Source
- `firebase-config.js` = Firebase Datenbank-Verbindung
- `shared.js` = gemeinsame Timer-Logik

## Wichtig
Vor dem Upload musst du in `firebase-config.js` deine Firebase Daten eintragen.
